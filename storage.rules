rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    // Allow all authenticated users to read and write all files
    // This is for development/testing - restrict later as needed
    match /{allPaths=**} {
      allow read, write: if request.auth != null;
    }
  }
}
